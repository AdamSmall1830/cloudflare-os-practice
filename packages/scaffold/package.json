{
  "name": "@cfos-practice/scaffold",
  "private": true,
  "version": "0.1.0",
  "description": "CLI: turn a Studio client export (JSON) into the full Cloudflare OS deployment kit — deployment.jsonc, SETUP.md, gatekeeper scaffolds, skills, eval suites, workflow specs, metrics log, and security baseline.",
  "type": "module",
  "bin": {
    "cfos-scaffold": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@cfos-practice/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
