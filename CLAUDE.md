# CLAUDE.md

Read **AGENTS.md** in this directory — it is the complete operating manual for AI agents working in this repo (mental model, data schema, engine API, extension recipes, and the hard rules). Follow it.

Quick facts: `pnpm check` must pass before any commit · `packages/core` is canonical, `studio/index.html` mirrors it by hand — change both · platform facts are pinned to Cloudflare OS v2 (Aug 2026); verify against the release in use before asserting them.
